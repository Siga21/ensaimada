{% extends "BackendBundle::backend.html.twig" %}
{% block id 'Cliente' %}
{% block title %}Añadir un nuevo cliente{% endblock %}

{% block article %}
<h1>{{ block('title') }}</h1>


<form action="{{ path('cliente_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="boton">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cliente') }}" class="boton">
            Volver a la lista
        </a>
    </li>
</ul>
{% endblock %}
