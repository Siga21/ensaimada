{% extends "BackendBundle::backend.html.twig" %}
{% block id 'Pedidod' %}
{% block title %}Añadir un nuevo pedidod{% endblock %}

{% block article %}
<h1>{{ block('title') }}</h1>


<form action="{{ path('pedidod_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="boton">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('pedidod') }}" class="boton">
            Volver a la lista
        </a>
    </li>
</ul>
{% endblock %}
