{% if paginador.currentPage('tienda') > 1 %}
   <a href="{{ path(ruta, {'page': paginador.previousPage('tienda'), 'paginatorId': 'tienda'}) }}"><img src="{{ asset('bundles/frontend/images/prev.png') }}" class="flechas"></a>
{% else %}
   {#<img src="{{ asset('bundles/frontend/images/prev.png') }}" class="flechas">#}
{% endif %}
{% if paginador.currentPage('tienda') < paginador.lastPage('tienda') %}
   <a href="{{ path(ruta, {'page': paginador.nextPage('tienda'), 'paginatorId': 'tienda'}) }}"><img src="{{ asset('bundles/frontend/images/next.png') }}" class="flechas"></a>
{% else %}
    {#<img src="{{ asset('bundles/frontend/images/next.png') }}" class="flechas">#}
{% endif %}

